db:
  restart: always
  image: postgres:9.6.2 // postgres 镜像
  ports:
    - 5432:5432  //映射端口号
  environment:
    POSTGRES_PASSWORD: ${pwd}  //密码
    PGDATA: /var/lib/postgresql/data/pgdata //数据存储文件夹
  volumes:
    - ./data:/var/lib/postgresql/data/pgdata //将数据映射到对应的路径